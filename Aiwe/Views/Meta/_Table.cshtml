@using Aiwe.Models.DB
@model List<MetaItem>
<table class="table table-bordered table-striped">
  <tr>
    <th>No</th>
    <th>Table Name</th>
    <th>Display Name</th>
    <th>Actions</th>
  </tr>

  @if (Model != null) {
    for (int i = 0; i < Model.Count(); ++i) {
      <tr>
        <td>@((i + 1).ToString())</td>
        <td>@Model[i].TableName</td>
        <td>@Model[i].DisplayName</td>
        <td>@Html.ActionLink(Aibe.LCZ.W_Edit, Aibe.DH.EditActionName, new { id = Model[i].TableName }) |
        @Html.ActionLink(Aibe.LCZ.W_Details, Aibe.DH.DetailsActionName, new { id = Model[i].TableName }) |          
          @Html.ActionLink(Aibe.LCZ.W_Delete, Aibe.DH.DeleteActionName, new { id = Model[i].TableName }) |
        @Html.ActionLink("Crypto-Serialize", "CryptoSerialize", new { id = Model[i].TableName })
        </td>
      </tr>
    }
  }
</table>

